Description: <short summary of the patch>
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 php5 (5.6.40+dfsg-0+deb8u4+deb9hetz1~exp6) stretch-hetz-experimental; urgency=medium
 .
   * Patch configure to cater for newer libcurl4-openssl-dev pathing
Author: Walter Kleynscheldt <walter.kleynscheldt@hetzner.co.za>

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: 2019-06-26

--- php5-5.6.40+dfsg.orig/configure
+++ php5-5.6.40+dfsg/configure
@@ -29607,12 +29607,17 @@ if test "$PHP_CURL" != "no"; then
   else
     { $as_echo "$as_me:${as_lineno-$LINENO}: checking for cURL in default path" >&5
 $as_echo_n "checking for cURL in default path... " >&6; }
-    for i in /usr/local /usr; do
+    for i in /usr/local /usr /usr/include/x86_64-linux-gnu; do
       if test -r $i/include/curl/easy.h; then
         CURL_DIR=$i
         { $as_echo "$as_me:${as_lineno-$LINENO}: result: found in $i" >&5
 $as_echo "found in $i" >&6; }
         break
+      elif test -r $i/curl/easy.h; then
+        CURL_DIR=$i
+        { $as_echo "$as_me:${as_lineno-$LINENO}: result: found in $i" >&5
+$as_echo "found in $i" >&6; }
+        break
       fi
     done
   fi
@@ -29630,10 +29635,10 @@ $as_echo_n "checking for cURL 7.10.5 or
 
   if ${CURL_DIR}/bin/curl-config --libs > /dev/null 2>&1; then
     CURL_CONFIG=${CURL_DIR}/bin/curl-config
-  else
-    if ${CURL_DIR}/curl-config --libs > /dev/null 2>&1; then
+  elif ${CURL_DIR}/curl-config --libs > /dev/null 2>&1; then
       CURL_CONFIG=${CURL_DIR}/curl-config
-    fi
+  elif /usr/bin/curl-config --libs > /dev/null 2>&1; then
+    CURL_CONFIG=/usr/bin/curl-config
   fi
 
   curl_version_full=`$CURL_CONFIG --version`
